import { RainbowdirDirective } from './rainbowdir.directive';

describe('RainbowdirDirective', () => {
  it('should create an instance', () => {
    const directive = new RainbowdirDirective();
    expect(directive).toBeTruthy();
  });
});
